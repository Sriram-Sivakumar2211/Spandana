--extra-index-url https://download.pytorch.org/whl/cpu

numpy==2.2.6
pandas==2.3.3
scipy==1.15.3
scikit-learn==1.7.2
PyWavelets==1.8.0
matplotlib==3.10.9
seaborn==0.13.2
# tensorboard removed - only needed for training, not deployment
tqdm==4.69.1
joblib==1.5.1
pyarrow==20.0.0
torch==2.7.1+cpu
ncps==0.0.7
Pillow==11.2.1
jsonschema==4.24.0
fastapi==0.115.12
uvicorn==0.34.3
python-dotenv==1.1.0
click==8.1.8

# Optional: enables the real Gemini-backed maintenance report generator in
# genai/llm_generator.py. Without it (or without GEMINI_API_KEY set), the
# backend automatically falls back to the deterministic grounded rule engine.
google-generativeai==0.8.5
protobuf==5.29.4
